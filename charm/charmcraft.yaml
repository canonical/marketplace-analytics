name: marketplace-analytics

type: charm

bases:
  - build-on:
    - name: ubuntu
      channel: "22.04"
    run-on:
    - name: ubuntu
      channel: "22.04"

summary: Marketplace Analytics service.

description: |
  Analytics service for the Canonical marketplace.

extensions:
  - flask-framework

requires:
  postgresql:
    interface: postgresql_client
    optional: false
